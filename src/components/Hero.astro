---
import Badge from "./ui/Badge.astro";
import { Play } from '@lucide/astro';

const isVisible = true;
---
{/* Hero Section */}
      <section class="relative bg-gradient-to-br from-[#0a30b8] to-[#0a30b8]/80 text-white py-20">
        <div class="container mx-auto px-4">
          <div class="grid lg:grid-cols-2 gap-12 items-center">
            <div class={`space-y-6 ${isVisible ? "animate-in slide-in-from-left duration-1000" : "opacity-0"}`}>
              <Badge description="Servicio"/>
              <h1 class="text-4xl lg:text-6xl font-bold leading-tight">
                Capacitación en <span class="text-blue-300">Seguridad y Salud</span> en el Trabajo
              </h1>
              <p class="text-xl text-white/90 leading-relaxed">
                En CENITEG SOLUTIONS, ofrecemos servicios de capacitación diseñados para garantizar el cumplimiento de
                la normativa vigente y promover una sólida cultura de prevención en su organización.
              </p>
              <div class="flex flex-col sm:flex-row gap-4">
                <button class="flex gap-2 p-2 rounded-3xl bg-white text-[#0a30b8] hover:bg-white/90 cursor-pointer">
                  <Play />
                  Ver Metodología
                </button>
                <button
                  class="rounded-3xl p-2 border-white text-white hover:bg-white hover:text-[#0a30b8] cursor-pointer"
                >
                  Solicitar Cotización
            </butto>
              </div>
            </div>
            <div class={`relative ${isVisible ? "animate-in slide-in-from-right duration-1000" : "opacity-0"}`}>
              <div class="relative rounded-2xl overflow-hidden shadow-2xl">
                <img
                  src="/images/vr-training-1.png"
                  alt="Capacitación con Realidad Virtual"
                  width={600}
                  height={400}
                  class="object-cover w-full h-[400px]"
                />
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent" />
                <div class="absolute bottom-4 left-4 text-white">
                  <p class="font-semibold">Tecnología VR Innovadora</p>
                  <p class="text-sm opacity-90">Experiencias inmersivas de seguridad</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>